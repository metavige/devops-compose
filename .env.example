# DevOps Compose 環境變數設定範本
# 複製此檔案為 .env 並填入適當的值

# === 基本配置 ===
MY_DOMAIN=docker.internal
DEFAULT_NETWORK=devops
BASE_NETWORK_YAML="docker-compose.network.yaml"

# === Infrastructure Layer 版本配置 ===
# Network Layer
TRAEFIK_VERSION=v3.5
DNSMASQ_VERSION=latest
WHOAMI_VERSION=latest

# Storage Layer
POSTGRES_VERSION=16
REDIS_VERSION=7-alpine
PGADMIN_VERSION=latest
REDIS_COMMANDER_VERSION=latest

# Security Layer
KEYCLOAK_VERSION=26.3.0

# === 日誌和除錯設定 ===
LOG_LEVEL=INFO
TRAEFIK_API_INSECURE=false

# === DNS 服務設定 ===
DNSMASQ_USER=admin
DNSMASQ_PASS=change_this_password

# === PostgreSQL 設定 ===
POSTGRES_USER=postgres
POSTGRES_DB=postgres
PGADMIN_EMAIL=admin@example.com

# === Redis 設定 ===
REDIS_PASSWORD=change_this_redis_password
REDIS_MAX_MEMORY=256mb

# === Keycloak 設定 ===
KEYCLOAK_ADMIN=admin
KEYCLOAK_DB=keycloak
KEYCLOAK_DB_USER=keycloak

# === SSL/TLS 設定 ===
ACME_EMAIL=admin@example.com

# === 效能調優 ===
# 可根據系統資源調整
POSTGRES_MAX_CONNECTIONS=100
REDIS_TIMEOUT=0