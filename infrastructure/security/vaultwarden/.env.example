# Vaultwarden 環境變數配置範例
# 複製此檔案為 .env 並填入實際值

# ===== 基礎設定 =====
MY_DOMAIN=docker.internal
DEFAULT_NETWORK=devops
TZ=Asia/Taipei

# ===== 註冊設定 =====
# 是否允許新用戶註冊 (首次設定建議開啟，之後建議關閉)
VAULTWARDEN_SIGNUPS_ALLOWED=true
# 是否允許邀請新用戶
VAULTWARDEN_INVITATIONS_ALLOWED=true
# 是否顯示密碼提示
VAULTWARDEN_SHOW_PASSWORD_HINT=false

# ===== 管理員設定 =====
# 管理員令牌 (用於訪問 /admin 管理介面)
# 生成方式: docker exec -it vaultwarden /vaultwarden hash
# 或使用: openssl rand -base64 48
VAULTWARDEN_ADMIN_TOKEN=

# ===== SMTP 郵件設定 (用於邀請和密碼重設) =====
VAULTWARDEN_SMTP_HOST=
VAULTWARDEN_SMTP_FROM=
VAULTWARDEN_SMTP_PORT=587
VAULTWARDEN_SMTP_SECURITY=starttls
VAULTWARDEN_SMTP_USERNAME=
VAULTWARDEN_SMTP_PASSWORD=

# ===== 資料庫設定 =====
# 預設使用 SQLite，如需使用 PostgreSQL 請取消註解並配置
# POSTGRES_USER=vaultwarden
# POSTGRES_PASSWORD=your_secure_password
# POSTGRES_DB=vaultwarden

# ===== 安全性設定 =====
# PBKDF2 迭代次數 (預設 600,000)
VAULTWARDEN_PASSWORD_ITERATIONS=600000

# ===== 備份設定 =====
# Rclone 遠端名稱 (local 表示本地備份)
VAULTWARDEN_RCLONE_REMOTE_NAME=local
# 備份目錄
VAULTWARDEN_RCLONE_REMOTE_DIR=/backup
# 備份排程 (Cron 格式，預設每天凌晨 2 點)
VAULTWARDEN_BACKUP_CRON=0 2 * * *
# 備份壓縮檔密碼
VAULTWARDEN_BACKUP_PASSWORD=
# 保留備份天數
VAULTWARDEN_BACKUP_KEEP_DAYS=30
